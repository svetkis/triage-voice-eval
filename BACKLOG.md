# Backlog — техдолг и архитектурные задачи

Статусы: `[ ]` todo · `[~]` in progress · `[-]` отклонено (с причиной)

---

## Правила для исполнителя (агента или человека)

### Команды верификации
```bash
make test              # основной прогон, должен быть зелёным после каждого тикета
pytest -v              # то же напрямую
make example-shopco    # smoke-check примера после изменений в core/runner/guards
```
Линтеров/type-checker в репо пока нет — не тащим их в рамках этих тикетов.

### Что НЕ трогать без явной необходимости
- **Публичное API** — список в [src/triage_voice_eval/__init__.py](src/triage_voice_eval/__init__.py#L3-L32). Переименования/удаления — только с записью в [CHANGELOG.md](CHANGELOG.md) и пометкой BREAKING.
- **Формат `result.json`** — существующие файлы в `runs_dir` должны продолжать читаться.
- **Сигнатура `EvalRunner.run`** — её вызывают пользователи. Добавлять параметры можно только с дефолтом.
- **`pyproject.toml` deps** — не добавлять рантайм-зависимости без обсуждения. Dev-зависимости — можно.

### Процесс
1. Один тикет = одна ветка `fix/NN-slug` (где NN — номер тикета).
2. После фикса: `pytest -v` зелёный + удалить пункт из беклога (или пометить `[-]` с причиной).
3. Если тикет требует решения не из беклога — **остановись и спроси**, не выбирай наугад.
4. Ломающие изменения → строка в CHANGELOG.md под `## [Unreleased]`.

---

## Отложено

### [-] #18 Большие прогоны: bounded producer
Отложено до реального юзкейса. 100k tasks в памяти ≈ 50MB, никто не жалуется. Если поднимется — переписать через `asyncio.Queue` + воркеры размера `concurrency` в [src/triage_voice_eval/runner.py](src/triage_voice_eval/runner.py).

---

## Архитектурные — требуют brainstorming

### [ ] #A1 Guards декларируют схему `case.expected`
Опечатки (`isCrisis` vs `is_crisis`) молча дают MISS. Идея: каждый Guard имеет `class ExpectedSchema(BaseModel)`, Scenario валидируется против объединения схем всех guards при старте раннера.
**Требует обсуждения:** как быть с guards, которые не читают `expected` (JailbreakGuard)? Опциональная схема? Объединение как union, или проверка только тех ключей, которые декларированы?

### [ ] #A2 `RunResult.results` — плоская структура
Двухуровневый nested dict плохо ложится на pandas/фильтры. Плоский `list[CaseRow]` удобнее для аналитики.
**Требует обсуждения:** ломаем формат `result.json` или добавляем параллельный `to_rows()` / `to_dataframe()` метод без изменения внутреннего представления?

---

## Мелочи, всплывшие по ходу (не из исходного ревью)

### [ ] Pytest PytestCollectionWarning про `TestCase`
`core.models.TestCase` конфликтует с pytest-конвенцией именования тест-классов (pytest пытается собрать модель как тесткейс). Четыре предупреждения при каждом прогоне. Варианты:
- Переименовать `TestCase` → `EvalCase` / `Case` / `TestExample` (BREAKING — публичный тип).
- Добавить `filterwarnings` в `[tool.pytest.ini_options]` в pyproject.toml (замалчивание).
- `pytest --no-header -p no:cacheprovider` — не помогает, не про это.

Сейчас: терпим 4 warning'а, не критично. Если соберётся будущий мажор — переименовать.
